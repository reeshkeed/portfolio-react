import { Navbar } from '@components/common/Navbar/Navbar';
import { Landing } from './components/Landing';

export const Home = () => {
  return (
    <main>
      <Navbar />

      <Landing />
    </main>
  );
};
